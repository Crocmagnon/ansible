- name: update caddy config
  hosts: servers
  become: true
  tasks:
    - name: write Caddyfile
      copy:
        src: ../../files/Caddyfile
        dest: /etc/caddy/Caddyfile
      notify:
        - reload caddy
  handlers:
    - name: reload caddy
      service:
        name: caddy
        state: reloaded
