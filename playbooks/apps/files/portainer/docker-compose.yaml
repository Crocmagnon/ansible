services:
  portainer:
    image: portainer/portainer-ee:latest
    restart: always
    volumes:
      - ./portainer_data:/data
      - /var/run/docker.sock:/var/run/docker.sock
    ports:
      - "9001:9443"
